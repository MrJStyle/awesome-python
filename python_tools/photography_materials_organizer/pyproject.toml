[project]
name = "media-organizer"
version = "1.0.0"
description = "媒体文件整理工具 - 按日期和设备名称自动整理视频和图片文件"
authors = [
    { name = "MrJStyle" }
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "gradio>=4.0.0",
]

[project.optional-dependencies]
dev = []
